{
    "version": 2,
    "builds": [
      { "src": "/backend/**", "use": "@vercel/node" },
      { "src": "/src/**", "use": "@vercel/static" }
    ],
    "routes": [
      { "src": "/backend/(.*)", "dest": "backend/$1" },
      { "src": "/(.*)", "dest": "/src/$1" }
    ]
  }